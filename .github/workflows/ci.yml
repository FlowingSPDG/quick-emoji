name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Run type checking
      run: npm run type-check

    - name: Run linting
      run: npm run lint

    - name: Run tests
      run: npm test

    - name: Build web app
      run: npm run build

  # E2E tests are disabled due to long execution time (10+ minutes)
  # Uncomment and configure timeout if needed:
  # e2e:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   timeout-minutes: 30
  #
  #   steps:
  #   - uses: actions/checkout@v4
  #
  #   - name: Use Node.js 20.x
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: 20.x
  #       cache: 'npm'
  #
  #   - name: Install dependencies
  #     run: npm install
  #
  #   - name: Install Playwright
  #     run: npx playwright install --with-deps
  #
  #   - name: Build web app
  #     run: npm run build
  #
  #   - name: Run E2E tests
  #     run: npm run test:e2e
  #
  #   - uses: actions/upload-artifact@v4
  #     if: always()
  #     with:
  #       name: playwright-report
  #       path: playwright-report/
  #       retention-days: 30